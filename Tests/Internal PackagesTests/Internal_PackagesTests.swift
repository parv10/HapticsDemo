@testable import Internal_Packages
import XCTest

final class Internal_PackagesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Internal_Packages().text, "Hello, World!")
    }
}
